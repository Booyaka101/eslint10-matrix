const plugin = require('@typescript-eslint/eslint-plugin');

const parser = { meta: { name: 'typescript-eslint/parser', version: '8.70.1' }, parseForESLint() {} };

module.exports = {
  plugin,
  parser,
  configs: { base: { name: 'typescript-eslint/base', languageOptions: { parser }, plugins: { '@typescript-eslint': plugin } } },
};
